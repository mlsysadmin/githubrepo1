apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: githubrepo
  labels:
    app: myapp
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 9000
  selector:
    app: myapp
